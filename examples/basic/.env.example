# Project API key (Dashboard → Settings → API keys). Unset ⇒ the SDK is a
# silent no-op and the example still runs.
FOGLAMP_API_KEY=

# Ingest endpoint. Defaults to the hosted endpoint; point at your own
# apps/ingest when self-hosting.
FOGLAMP_INGEST_URL=http://localhost:4000/ingest
